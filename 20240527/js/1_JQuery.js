$(document).ready(function () {
  $('body').css('backgroundColor', 'yellow');

  // h1 요소의 글자색을 파란색으로 변경
  $('h1').css('color', 'blue');
  // id가 smTitle인 요소의 글자색을 초록색으로 변경
  $('#smTitle').css('color', 'green');
  // class가 sample인 요소의 글자색을 빨강색으로 변경
  $('.sample').css('color', 'red');
});
